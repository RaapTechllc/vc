# VC Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git (it's in .gitignore)

# ============================================================================
# Authentication (choose one):
# ============================================================================

# Option 1: OAuth Token (RECOMMENDED for Claude subscription users)
# This uses your Claude Pro/Max subscription quota instead of API billing
#
# How to get your OAuth token:
#   1. Run this command in PowerShell or terminal:
#      claude setup-token
#   2. Follow the interactive prompts to authenticate
#   3. Copy the token that's displayed (starts with sk-ant-oat01-)
#   4. Paste it below (uncomment the line first)
#
# ANTHROPIC_OAUTH_TOKEN=sk-ant-oat01-xxxxx

# Option 2: API Key (traditional method for API users)
# Uses pay-per-token API billing (separate from subscription)
#
# How to get your API key:
#   1. Visit https://console.anthropic.com/settings/keys
#   2. Create a new API key
#   3. Paste it below (uncomment the line first)
#
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# ============================================================================
# Optional Configuration
# ============================================================================

# Database path (optional, auto-discovers if not set)
# VC_DB_PATH=.beads/beads.db

# AI Model Configuration (optional)
# VC_MODEL_DEFAULT=claude-sonnet-4-5-20250929
# VC_MODEL_SIMPLE=claude-3-5-haiku-20241022

# Debug Flags (optional)
# VC_DEBUG_PROMPTS=1      # Log full prompts sent to AI
# VC_DEBUG_EVENTS=1       # Log agent progress events
# VC_DEBUG_STATUS=1       # Log issue status changes
